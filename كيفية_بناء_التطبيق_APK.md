# دليل بناء تطبيق أندرويد (APK) باستخدام Capacitor على جهازك

هذا الدليل يحل المشاكل الشائعة التي تواجهها أثناء بناء تطبيق الـ APK على نظام تشغيل Windows.

---

## 🛠️ المشكلة الأولى: اسم المجلد يحتوي على حروف عربية (المجلد اسمه "رفيق")
### سبب المشكلة:
برنامج البناء أندرويد (Gradle) على نظام Windows لا يدعم المسارات التي تحتوي على حروف عربية أو خاصة (مثل `رفيق` أو اسم المستخدم `فتون`).

### الحل الأسهل والأسرع (موصى به):
1. قم بإغلاق سطر الأوامر (Command Prompt / PowerShell).
2. اذهب إلى مجلد مشروعك الحالي على سطح المكتب.
3. قم بنقله أو إعادة تسميته بحيث يكون في مسار يحتوي على حروف إنجليزية فقط وبدون مسافات.
   * **مثال ممتاز:** قم بنقل مجلد المشروع بالكامل وضعه مباشرة داخل القرص `C:` وغير اسمه ليصبح: `C:\Rafeeq`
4. افتح سطر الأوامر من جديد واذهب للمسار الجديد بكتابة:
   ```bash
   cd C:\Rafeeq
   ```

### الحل البديل (تخطي الفحص من الإعدادات):
إذا لم تكن تريد نقل المجلد أو تغيير اسمه، يمكنك إخبار أندرويد بتجاهل هذا الفحص:
1. افتح مجلد مشروعك، ثم ادخل إلى مجلد `android` ثم مجلد `gradle.properties`.
2. افتح ملف `gradle.properties` باستخدام **برنامج المفكرة (Notepad)**.
3. أضف السطر التالي في نهاية الملف تماماً في سطر جديد:
   ```properties
   android.overridePathCheck=true
   ```
4. احفظ الملف واغلقه، ثم جرب بناء المشروع مجدداً.

---

## 🔄 المشكلة الثانية: خطأ `android platform already exists`
### سبب المشكلة:
تظهر هذه الرسالة لأنك قمت بالفعل بإضافة منصة الأندرويد سابقاً، ولا يمكنك تشغيل أمر الإضافة `add android` مرتين متتاليتين.

### الحل:
لا تحتاج لتشغيل أمر الإضافة مجدداً! بدلاً من ذلك، استخدم الأوامر التالية للمزامنة والبناء مباشرة:
1. لبناء ملفات الويب:
   ```bash
   npm run build
   ```
2. لمزامنة الكود مع الأندرويد:
   ```bash
   npx cap sync
   ```
3. لفتح المشروع في Android Studio لإنتاج الـ APK:
   ```bash
   npx cap open android
   ```

---

## 🚫 المشكلة الثالثة: خطأ `Cannot run init for a project using a non-JSON configuration file`
### سبب المشكلة:
هذا الخطأ يظهر عند محاولة تهيئة Capacitor مجدداً في وجود ملف إعدادات بصيغة TypeScript (`capacitor.config.ts`).

### الحل:
قم بمسح ملف `capacitor.config.ts` ثم أعد المحاولة، أو تأكد من استخدام ملف `capacitor.config.json` بدلاً منه إذا كان مطلوباً، ولكن في الغالب لا تحتاج لتشغيل أمر التهيئة (init) مجدداً إذا كان المشروع مهيأً بالفعل.

---

## 🔒 المشكلة الرابعة: تحذير/خطأ `proguard-android.txt is no longer supported`
### سبب المشكلة:
في تحديثات أندرويد الحديثة (Gradle / R8)، تم إيقاف دعم ملف الحماية الافتراضي القديم `proguard-android.txt` لأنه يمنع تحسينات الأداء، ويُطلب الآن استخدام النسخة المحسّنة والمطوّرة بدلاً منه.

### الحل:
لتعديل هذا السطر وحل المشكلة بسهولة:
1. افتح مجلد مشروعك على جهازك.
2. اذهب إلى المسار التالي:
   `android` ⬅️ ثم `app` ⬅️ ثم ابحث عن ملف باسم `build.gradle` (انتبه: يوجد ملف آخر بنفس الاسم في المجلد الرئيسي، تأكد من فتح الملف الموجود داخل مجلد `app`).
3. افتح ملف `build.gradle` باستخدام **برنامج المفكرة (Notepad)** أو أي محرر نصوص.
4. ابحث داخل الملف عن هذا السطر (غالباً ستجده في السطر رقم 50 تقريباً أو تحت كلمة `buildTypes`):
   ```groovy
   getDefaultProguardFile('proguard-android.txt')
   ```
5. قم بتغييره ليصبح هكذا (أضف كلمة `-optimize`):
   ```groovy
   getDefaultProguardFile('proguard-android-optimize.txt')
   ```
6. احفظ الملف واغلقه، ثم أعد عملية بناء التطبيق في Android Studio أو سطر الأوامر وسيعمل معك بنجاح!

